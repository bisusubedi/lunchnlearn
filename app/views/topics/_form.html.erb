<%= form_for(@topic) do |f| %>
<% if @topic.errors.any? %>
<div class="error_explanation alert alert-error">
	<h3><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h3>
		<ul>
		<% @topic.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<div class="row-fluid">
	<div class="span7">
		What topic you want to talk on :
		<br />
		<%= f.text_field :name,:class=>"input-xxlarge" %>
		<br />
		Highlights of your talk:
		<br />
		<%= f.text_area :description, :class=>"input-xxlarge",:rows=>"2" %>
		<br />
		<div class="">
		<%= f.submit("Post",:class=>"btn btn-primary") %>
	</div>
	</div>
	
</div>
</div>
<% end %> 